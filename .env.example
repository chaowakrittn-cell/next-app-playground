# Demo values for the "Environment Poisoning" showcase.
# SECRET_SERVER_VALUE has no NEXT_PUBLIC_ prefix → stripped from the client bundle.
SECRET_SERVER_VALUE=server-side-only-secret-123
# NEXT_PUBLIC_ prefix → inlined into the client bundle, readable in the browser.
NEXT_PUBLIC_DEMO_VALUE=safe-to-expose-456
